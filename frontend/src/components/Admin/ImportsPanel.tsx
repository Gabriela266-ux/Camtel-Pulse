import React from 'react';

export const ImportsPanel: React.FC = () => {
	return (
		<div>
			<h2 className="text-lg font-bold">Imports</h2>
			<p className="text-sm text-slate-600">Importer Balance Overview, afficher erreurs, lignes acceptées/refusées.</p>
			<div className="mt-4 rounded border bg-white p-4">(Uploader + aperçu des erreurs - à implémenter)</div>
		</div>
	);
};

export default ImportsPanel;
